# خطاهای شناخته‌شده

هر مورد با شواهد قابل‌بازتولید ثبت می‌شود. موردی که شواهد ندارد اینجا نوشته
نمی‌شود.

## BUG-001 — انحراف overlay استراتژی Delta را بی‌صدا خاموش می‌کند

- شدت: بالا (عملیاتی)
- وضعیت: تشخیص داده شد؛ کد رصد اضافه شد، اصلاح فایل روی هاست باقی است
- مراحل بازتولید: در `config/runtime_overrides.yaml` مقدار
  `strategy.enabled_strategies` را روی `[liquidity_volume]` بگذارید و ربات را
  اجرا کنید.
- رفتار مورد انتظار: چون `config/settings.yaml` مقدار `delta` را در
  `enabled_strategies` دارد، Delta باید فعال باشد.
- رفتار فعلی: `resolve_enabled_strategies`
  (`src/chronoscalp/strategy/multi_timeframe.py`) وقتی لیست موجود باشد فقط
  همان لیست را می‌خواند، پس overlay استراتژی را کاملاً حذف می‌کند و هیچ لاگی
  این را اعلام نمی‌کرد.
- کاهش اثر: حلقه‌ی زنده حالا موقع استارت «Entry gate profile» را لاگ می‌کند و
  اگر هیچ استراتژی فعال نباشد WARNING می‌دهد.

## BUG-002 — یازده کلید runtime override اعتبارسنجی می‌شوند ولی اعمال نمی‌شوند

- شدت: بالا (ایمنی کاذب)
- وضعیت: مستند شد؛ تصمیم پیاده‌سازی یا حذف باقی است
- مراحل بازتولید: هر کلید فهرست‌شده در
  `chronoscalp.config_overrides.UNENFORCED_OVERRIDE_KEYS` را در overlay
  تنظیم کنید و در `src/` دنبال خواننده‌اش بگردید.
- رفتار مورد انتظار: سقف معاملات روزانه، حرارت پرتفوی، سقف ضرر هفتگی و
  ماهانه، fail-closed خبری و قفل تک‌نمونه باید اعمال شوند.
- رفتار فعلی: هیچ مسیر کدی خارج از `config_overrides.py` این کلیدها را
  نمی‌خواند.
- کاهش اثر: در فایل نمونه با `# INERT` علامت خورده‌اند و موقع استارت با
  WARNING فهرست می‌شوند.

## BUG-003 — چرن اتصال MT5

- شدت: متوسط
- وضعیت: اصلاح شد (دو مرحله)
- مراحل بازتولید: یک `MT5Connector` **تازه** بسازید و `connect()` بزنید، در
  حالی که همان پروسه از قبل به ترمینال وصل است.
- رفتار مورد انتظار: لینک موجود دوباره استفاده شود.
- رفتار فعلی (پیش از اصلاح): هر تلاش اول `mt5.shutdown()` می‌زد، پس هر
  فراخوان تکراری لینک IPC را وسط دریافت قیمت یا ارسال سفارش بازمی‌ساخت.
- اصلاح مرحله‌ی اول: نگهبان idempotent روی `self._connected`.
- چرا کافی نبود: پس از دیپلوی، لاگ VPS در ۱۶۴ خط ۷۰ جفت
  `Connecting/Connected` داشت با `terminal_info() is None` برابر صفر — یعنی
  لینک سالم بود و صرفاً نمونه‌های تازه ساخته می‌شدند. `saas/broker_wizard.py`
  و مسیرهای پنل/تلگرام به‌ازای هر درخواست یک `MT5Connector` یک‌بارمصرف
  می‌سازند، پس پرچم instance-scoped همیشه `False` است.
- اصلاح مرحله‌ی دوم: `_process_link_matches()` وضعیت را از خود پکیج
  `MetaTrader5` می‌خواند (یک لینک به‌ازای هر پروسه) و فقط وقتی
  `account_info().login` با لاگین درخواستی یکی باشد از دست‌دادن دوباره صرف‌نظر
  می‌کند. `force=True` برای بازسازی عمدی.

## BUG-004 — R واقعی در رکوردهای جولای ۲۰۲۶ نامعتبر است

- شدت: متوسط (تحلیل، نه اجرا)
- وضعیت: کد اصلاح شده؛ داده‌ی تاریخی همچنان خراب است
- شواهد: در `data/_analysis/trade_journal_live.json` میانگین `r_multiple`
  حدود ۱.۴ میلیون است و ۱۷۸ رکورد |R| بالای ۱۰ دارند.
- رفتار فعلی: `TradeJournal` حالا R را از ریسک دلاری حساب می‌کند و |R|>10 را
  با پرچم `implausible_r` صفر می‌کند، ولی رکوردهای قدیمی بازنویسی نشده‌اند.
- نتیجه: هیچ آمار expectancy‌ای که از رکوردهای جولای گرفته شود قابل‌اتکا
  نیست.

## BUG-005 — سه نماد از چهار نماد فعال اصلاً قابل معامله نیستند

- شدت: بالا (توضیح مستقیم «تعداد معاملات کم»)
- وضعیت: تشخیص داده شد با شواهد؛ تصمیم اصلاح با مالک پروژه است
- شواهد: heartbeat سرور در ۱۷ آگوست ۲۰۲۶ (هر پنجره ۳۰۰ ثانیه)، با
  `symbols: [XAUUSD, BTCUSD, EURUSD, USDJPY]`:
  - `BTCUSD:no_trigger_data=22..31` — هیچ کندلی از بروکر نمی‌آید. نماد روی
    حساب `AUSCommercial-Demo` با این نام وجود ندارد.
  - `EURUSD` و `USDJPY`: هر ارزیابی با
    `scalp:symbol_blocked` (ultra_scalp برای این نمادها غیرفعال است) به‌علاوه
    `delta:symbol_blocked` (Delta فقط XAUUSD مجاز است) رد می‌شود. هیچ
    استراتژی فعالی برای این دو نماد باقی نمی‌ماند.
  - `XAUUSD` تنها نماد قابل‌معامله است و با `delta:regime_neutral` و
    `spread_ma` رد می‌شود.
- نتیجه‌ی کمی: در ۱۶ آگوست ۲۰۲۶ دقیقاً یک معامله ثبت شد
  (XAUUSD buy 2.0 lot @ 4383.77، ticket 73715483، سود ۶۴.۰۰).
- اصلاح اعمال‌شده (۱۷ آگوست، با تأیید مالک پروژه): `symbols` به
  `[XAUUSD, EURUSD]` کاهش یافت، `strategy.delta.allowed_symbols` مقدار
  پیش‌فرض کد یعنی `[XAUUSD, EURUSD]` را پس گرفت، و `ultra_scalp` از
  `enabled_strategies` حذف شد. هیچ گاردی شل نشد.
- هشدار: شل‌کردن گارد اسپرد یا فیلتر رژیم Delta برای بالابردن تعداد معامله
  دقیقاً همان کاری است که `AGENTS.md` منع می‌کند.

## BUG-006 — قیف ورود هفتگی: اسپرد و three_strikes بزرگ‌ترین سدها هستند

- شدت: بالا (تصمیم تنظیم، نه نقص کد)
- وضعیت: اندازه‌گیری شد؛ تصمیم با مالک پروژه است
- ابزار: `scripts/analyze_spread_guard.py` روی هفت فایل لاگ
  (۱۱ تا ۱۷ آگوست ۲۰۲۶). خروجی در
  `data/reports/spread_guard_report.json`.
- XAUUSD — ۲۲٬۵۱۰ ارزیابی رد شده:

  | دلیل | تعداد | سهم |
  |---|---|---|
  | `scalp:symbol_blocked` (ultra_scalp، حالا حذف شده) | ۸٬۵۸۶ | ۳۸٪ |
  | `spread_ma` | ۵٬۴۸۸ | ۲۴٪ |
  | `three_strikes` | ۴٬۱۱۵ | ۱۸٪ |
  | رژیم/ترند خنثی Delta و institutional | ~۲٬۸۰۰ | ۱۲٪ |

- اسپرد طلا هنگام رد شدن: کمینه ۵، میانه ۱۱، صدک نود ۱۴، بیشینه ۲۶، در برابر
  سقف میانه‌ی ۷.۸۱ — یعنی نسبت مازاد ۱.۴۱.
- EURUSD به‌مراتب سالم‌تر است: گارد اسپرد فقط ۶.۹٪ از ردها را می‌سازد و اسپرد
  میانه هنگام رد ۰.۲۰ در برابر سقف ۰.۱۶ است. این پشتوانه‌ی افزودن EURUSD به
  `delta.allowed_symbols` است.
- `three_strikes` در `config/settings.yaml` روی `max_losses: 3` و
  `pause_hours: 12` است. روی رباتی که ۲۴ ساعته کار می‌کند هر بار فعال‌شدن
  نیم روز معامله را حذف می‌کند. این یک کنترل ریسک عمدی است و بدون تصمیم مالک
  پروژه تغییر نمی‌کند.
- محدودیت داده: فقط اسپردهای *رد شده* لاگ می‌شوند، پس صدک‌ها توزیع کامل
  اسپرد نیستند. برای تصمیم درباره‌ی ضریب گارد، به نمونه‌برداری از همه‌ی
  اسپردها نیاز است نه فقط ردها.

## نکته‌ی پیکربندی — بک‌تست معادل لایو نیست

خطا نیست ولی مکرراً باعث برداشت اشتباه شده: `backtest/engine.py` فقط سشن،
خبر، اسپرد و `validate_signal` را اعمال می‌کند. حلقه‌ی زنده نُه گارد ورود
دیگر دارد که در `backtest.engine.LIVE_ONLY_GATES` فهرست شده‌اند. تعداد
معاملات بک‌تست سقف بالای تعداد معاملات لایو است، نه پیش‌بینی آن.
